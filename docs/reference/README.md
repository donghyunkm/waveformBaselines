# Reference

This folder holds cross-cutting background material that is not owned by one experiment stream.

Key docs:

- `data_description.md`: general waveform input, split, normalization, and target-background notes.
