# Project Progress

Concise chronological project log. Detailed technical information lives in `docs/`.

## 2026-09-03

### Completed
- Started the confirmed-vasopressor-overlap full-data raw-waveform PatchTST workstream: saved anchor/normalization/target/numerics/submit scripts, built the anchor cache, regression target bundle, waveform normalization stats, aligned numerics cache, and onset event-target bundle. See `docs/full_data/full_data_raw_waveform_models_vasopressor_present.md`.
- Made the vasopressor-present event-target wrapper explicitly use the current full-data onset-label definition and strict filtered negative policy. See `docs/full_data/full_data_raw_waveform_models_vasopressor_present.md`.
- Added dedicated trailing-30-second `patchtst_v1_5` vaso-present submitters and dry-ran all requested 20-minute/30-second regression/classification batches. See `docs/full_data/full_data_raw_waveform_models_vasopressor_present.md`.
- Fixed the shared PatchTST SLURM wrapper so `--physiojepa-fidelity` v1.5 jobs no longer inherit conflicting architecture defaults; syntax checks, 30-second vaso-present dry-runs, and parser-level preflight passed. See `docs/full_data/full_data_raw_waveform_models_vasopressor_present.md`.
- Submitted all twenty requested vaso-present raw-waveform training jobs on `gl40s_short`: 20-minute `patchtst_v1` regression/classification and trailing-30-second `patchtst_v1_5` regression/classification. See `docs/full_data/full_data_raw_waveform_models_vasopressor_present.md`.
- Checked `cpu_short` partition limits (`MaxTime=12:00:00`), canceled the old pending vaso-present prep chain `26984462`-`26984465`, and resubmitted replacements `26985172`-`26985175` on `cpu_short` with the max time limit. See `docs/full_data/full_data_raw_waveform_models_vasopressor_present.md`.
- Moved the remaining ten `ptst_v15_30s_*` jobs from congested `a100_short` back to `gl40s_short`; latest `squeue` shows all still pending by priority. See `docs/full_data/full_data_30s_waveform_models.md`.
- Canceled active full-data combined v7+v8 extracted-feature SLURM jobs (`wf_feat_tab` and `wf_feat_model`) for now because of deadlines; partial artifacts exist but should not be interpreted as a complete result set. See `docs/full_data/extractedFeaturesRegressionFullData.md` and `docs/full_data/extractedFeaturesClassificationFullData.md`.
- Canceled non-priority raw-waveform regression SLURM jobs because of deadlines while keeping `MAP`, `PLETH_ACDC`, `ABP_tau`, and `dPdt_max` related work where present; canceled targets should be resumed soon. See `docs/full_data/full_data_raw_waveform_models.md` and `docs/full_data/full_data_30s_waveform_models.md`.
- Completed repo handoff audit against live SLURM state, key outputs, logs, scripts, and docs. `git status` could not be checked because `git` is not installed on this node.
- Checked current job progress at `09:53 EDT`: full-data 20-minute classification now has checkpoints and evaluation exports for all six event targets, and final evals completed for `PLETH_ACDC` and `ABP_tau`. See `docs/full_data/full_data_raw_waveform_models.md`.
- Confirmed the old vasopressor-free 30-second `patchtst_v1_5` jobs `26981238`, `26981240`, `26981245`, `26981247`, `26981249`, `26981251`, `26981253`, `26981254`, `26981255`, and `26981256` failed immediately with the pre-fix `--physiojepa-fidelity` architecture-default conflict; they should be resubmitted with the corrected wrapper. See `docs/full_data/full_data_30s_waveform_models.md`.
- Resubmitted the failed vasopressor-free 30-second `patchtst_v1_5` kept jobs with the corrected wrapper: regression `26988945`-`26988948` and classification `26988951`-`26988956`. See `docs/full_data/full_data_30s_waveform_models.md`.
- Added partition/job-name/log-name/node-exclusion override hooks to the vaso-present submitters and submitted distinct `a100_short` copies of the twenty vaso-present jobs without touching the existing `gl40s_short` queue. Some first copies landed on `a100-4012`/`a100-4021` and failed CUDA initialization, then were replaced with excluded-node submissions. See `docs/full_data/full_data_raw_waveform_models_vasopressor_present.md`.
- Canceled the original pending vaso-present `gl40s_short` jobs `26985976`-`26985995` after confirming the distinct `a100_short` copies were active/queued. See `docs/full_data/full_data_raw_waveform_models_vasopressor_present.md`.
- Checked current SLURM progress at `10:30 EDT`: identified `a100-4028` as another CUDA-unavailable node, updated remaining pending A100 jobs to exclude it, and resubmitted the failed vaso-present A100 copies as `26989262`-`26989276`. See `docs/full_data/full_data_raw_waveform_models_vasopressor_present.md`.

### In Progress
- Confirmed-vasopressor-overlap raw-waveform `a100_short` copies are active/queued with `_a100` job/log suffixes and separate `_a100` run tags: running jobs are `26988968` PLETH_ACDC, `26988969` ABP_tau, and `26988980` 30-second dPdt_max; pending jobs are `26989017` 20-minute hypoxia 10m, `26989018`-`26989019` 30-second MAP/PLETH_ACDC, and replacements `26989262`-`26989276`. See `docs/full_data/full_data_raw_waveform_models_vasopressor_present.md`.
- Full-data 20-minute raw-waveform `patchtst_v1` kept regression job `26957110` dPdt_max is running at epoch `33`; dependent eval `26981973` remains pending on dependency. See `docs/full_data/full_data_raw_waveform_models.md`.
- Full-data vasopressor-free 30-second `patchtst_v1_5` replacement jobs are queued on `gl40s_short` priority: regression `26988945` MAP, `26988946` PLETH_ACDC, `26988947` dPdt_max, `26988948` ABP_tau; classification `26988951`-`26988956`. See `docs/full_data/full_data_30s_waveform_models.md`.
- Two interactive shell jobs `26988908` and `26988909` are running on `cpu_dev`; they were not modified.

### Next Steps
1. Monitor vaso-present `a100_short` active copies `26988968`, `26988969`, `26988980`, `26989017`-`26989019`, and `26989262`-`26989276`; evaluate only after final checkpoints are written.
2. Keep the canceled `gl40s_short` vaso-present IDs `26985976`-`26985995` out of future active-job lists unless they are intentionally resubmitted.
3. Monitor `26957110`; after it completes, let/run dependent eval `26981973` so dPdt_max has aligned `test_metrics.json` and `test_predictions.npz`.
4. Monitor vasopressor-free 30-second replacements `26988945`-`26988948` and `26988951`-`26988956`; inspect logs once they start.
5. Resume canceled raw-waveform regression targets after deadline pressure clears.
6. Resume canceled full-data combined v7+v8 extracted-feature jobs as soon as possible, then audit partial artifacts before interpreting any results.
7. Run full-cache V8 QC/audit on `full_data_vasopressor_free_waveform_features_v8_segment_plan` before relying on v8 downstream.

## 2026-09-02

### Completed
- Prepared and submitted the full-data 4-channel trailing-30-second `patchtst_v1_5` raw-waveform workload: `26` regression jobs and `6` classification jobs, then moved them from `gl40s_short` to `a100_short`. See `docs/full_data/full_data_30s_waveform_models.md`.
- Added full-data trailing-window support for raw-waveform loading via `input_window_position=input_end`, wired it through PatchTST train/eval configs, added focused tests, and created dedicated 30-second v1.5 submitters. See `docs/full_data/full_data_30s_waveform_models.md`.
- Added a same-window v7 source-feature agreement audit comparing selected v7 features to historical source `X_stats` comparators, with code and JSON/CSV/Markdown outputs. See `docs/v7_extracted_features/extractedFeaturesVerification.md` and `outputs/feature_models/v7_source_agreement_2026-09-02.{json,csv,md}`.
- Added a v7 extracted-feature verification doc consolidating tests, smoke/full-cohort audits, v7 verification plots, plot-reading notes, and caveats. See `docs/v7_extracted_features/extractedFeaturesVerification.md`.
- Revised the v7 regression best-completed summary to report metric-specific winners for R2, RMSE, and MAE, and refreshed paired sequence-vs-nonsequence significance for GRU/Transformer metric wins. See `docs/v7_extracted_features/extractedFeaturesRegression.md` and `outputs/feature_models/v7_best_completed_regression_by_metric_2026-09-02.{json,csv,md}`.
- Added a compact v7-vs-PatchTST best-R2 improvement table with paired permutation p-values and an embedded horizontal bar chart. See `docs/v7_extracted_features/extractedFeaturesRegression.md` and `docs/figures/v7_best_r2_improvement_vs_patchtst.png`.
- Cancelled the old full V8 row-sharded SLURM array `26961873` and submitted optimized exact segment-planned V8 array `26968656` using `--use-reference-segment-component-plan`. See `docs/v7_extracted_features/v8_supplemental_features.md`.
- Completed final V8 exact-efficiency/profiling work before production extraction; no further feature-code changes were retained beyond measured exact optimizations. See `docs/v7_extracted_features/v8_supplemental_features.md`.
- Completed a fresh handoff audit of current SLURM state, V8 logs/output state, full-data PatchTST artifacts, and documentation state. See `docs/v7_extracted_features/v8_supplemental_features.md`, `docs/full_data/full_data_raw_waveform_models.md`, and `docs/full_data/full_data_30s_waveform_models.md`.
- Fixed V8 shard-aware v7 alignment validation, recovered all `128` completed segment-plan shards from SLURM array `26968656`, canceled pending merge job `26980077`, and completed the V8 merge locally. See `docs/v7_extracted_features/v8_supplemental_features.md`.
- Built a combined full-data v7+v8 extracted-feature cache and submitted regression/classification jobs for `history_xgb`, `full_sequence_xgb`, and `transformer`. See `docs/full_data/extractedFeaturesRegressionFullData.md` and `docs/full_data/extractedFeaturesClassificationFullData.md`.

### In Progress
- V8 merged full-data cache is available at `/gpfs/data/eh3828lab/derived_datasets/baselines/waveformBaselines/featureExtraction/full/v8/full_data_vasopressor_free_waveform_features_v8_segment_plan`; full-cache QC/audit is still pending. See `docs/v7_extracted_features/v8_supplemental_features.md`.
- Full-data v7+v8 extracted-feature jobs were canceled on 2026-09-03 for deadlines and should be resumed as soon as possible; no v7+v8 metrics or prediction exports should be interpreted until the resubmitted jobs complete and artifacts are audited. See `docs/full_data/extractedFeaturesRegressionFullData.md` and `docs/full_data/extractedFeaturesClassificationFullData.md`.
- Full-data 20-minute raw-waveform `patchtst_v1` regression jobs `26957107`-`26957112` are running on `gl40s_short`; regression jobs `26957113`-`26957119` and classification jobs `26957120`-`26957125` are pending on `gl40s_short` priority. Observed regression artifacts: `19/26` checkpoints and `8/26` evaluation exports. See `docs/full_data/full_data_raw_waveform_models.md`.
- Full-data 30-second raw-waveform `patchtst_v1_5` jobs are pending on `a100_short` priority: regression `26981233`-`26981244`, `26981246`, `26981248`, `26981250`, `26981252`, `26981254`, `26981256`-`26981264`; classification `26981245`, `26981247`, `26981249`, `26981251`, `26981253`, `26981255`. No 30-second output directories or logs were observed at handoff. See `docs/full_data/full_data_30s_waveform_models.md`.
- Non-repo-specific interactive shell jobs `26979253` and `26979256` (`bash`) are running on `cpu_dev`.
- Git working-tree status could not be checked because `git` is not installed on this node.

### Next Steps
1. Monitor the 30-second `patchtst_v1_5` jobs on `a100_short`; after checkpoints appear, evaluate `outputs/patchtst/full_data_v15_4ch_30s_es` and `outputs/patchtst/full_data_v15_4ch_30s_events_es` with `slurm/eval_patchtst.sh`.
2. Monitor full-data 20-minute raw-waveform `patchtst_v1` jobs; after remaining checkpoints complete, rerun/evaluate exports so all intended regression/classification targets have aligned `test_metrics.json` and `test_predictions.npz`.
3. Monitor full-data v7+v8 extracted-feature jobs; after completion, audit `metrics.json`, `test_predictions.npz`, and XGBoost `model.pkl` artifacts before interpreting results.
4. Run full-cache V8 QC/audit on `full_data_vasopressor_free_waveform_features_v8_segment_plan` before relying on v8 downstream.
5. Do not use partial raw-waveform or v7+v8 runs for significance or Bland-Altman comparisons until evaluation exports are complete and aligned.
6. Continue v7 model-analysis follow-up: review TCN narrative, decide how to display persistence despite seven alignment failures, and add selected classification calibration/threshold diagnostics.

## 2026-09-01

### Completed
- Optimized V8 runtime helper internals without changing the 194-feature schema: exact vectorized leave-one-out template distances, exact KD-tree SampEn, default-disabled PLETH derivative gating, and baroreflex lag-cache reuse. Focused V8 tests passed (`70` run, `69` passed, `1` skipped), full unittest discovery passed (`267` run, `266` passed, `1` skipped), compileall passed, and representative one-window timing improved from `24.125 s` to `19.501 s`; redundant detector calls remain the next bottleneck. See `docs/v7_extracted_features/v8_supplemental_features.md`.
- Completed end-of-session handoff audit at `2026-09-01 17:55 EDT`: checked live SLURM state, full-data PatchTST artifacts/logs, V8 extraction status, regenerated Bland-Altman outputs, and relevant docs. See `docs/full_data/full_data_raw_waveform_models.md`, `docs/v7_extracted_features/v8_supplemental_features.md`, and `docs/v7_extracted_features/bland_altman_analysis.md`.
- Started full default V8 revision-7 extraction after 10-sample QC: default QC cache `/tmp/waveform_feature_cache_v8_qc/v8/qc10_v8_revision7_default` and audit-enabled QC cache `/tmp/waveform_feature_cache_v8_qc/v8/qc10_v8_revision7_audit_enabled` both have shape `(10, 20, 194)` with v7-prefix alignment passing. Submitted SLURM array `26961873` (`2048` shards, `%128`) using `slurm/extract_full_data_vasofree_waveform_features_v8_array.sh`; merge support is in `scripts/merge_waveform_features_v8_shards.py` and `slurm/merge_full_data_vasofree_waveform_features_v8.sh`. See `docs/v7_extracted_features/v8_supplemental_features.md`.
- Applied the final V8 qualifying-run freeze fix: count-plus-coverage features now select the best continuous run among runs satisfying both requirements, `_longest_valid_rr_run()` accepts count/coverage filters, HRV/ABP/PLETH/RESP call sites were updated, HRV ratio and baroreflex sequence-fraction definitions were corrected, and the internal schema revision was bumped to `7` with hash `a72aeb3b2a2942a899851cffab6d43228e939a82653514907b4fe8748cae229b`. Feature count remains `194`; focused V8 tests passed (`67` run, `66` passed, `1` optional pyPPG hook skipped), full unittest discovery passed (`264` run, `263` passed, `1` skipped), and revision-7 smoke cache `/tmp/waveform_feature_cache_smoke_v8_revision7/v8/smoke_local_v8_revision7` has shape `(2, 20, 194)` with v7-prefix alignment passing. V8 is frozen; next step is representative extraction/QC and then full extraction. See `docs/v7_extracted_features/v8_supplemental_features.md`.
- Updated the V7-vs-PatchTST Bland-Altman analysis for v7 extracted-feature regression to use paired patient-cluster bootstrap CIs as the only inferential output, with GRU labeled as V7, undefined Within Tolerance comparison rows omitted, validation metadata, target-level summaries, and focused tests. See `docs/v7_extracted_features/extractedFeaturesRegression.md`, `docs/v7_extracted_features/bland_altman_analysis.md`, and `blandaltman_v7_extractedfeatures/bland_altman_paired_significance.{json,csv,md}`.
- Completed final V8 freeze cleanup: Lomb-HRV now uses `config.min_hrv_coverage_seconds`, generic SBP-RR definitions describe the selected sufficiently covered continuous ABP population, feature count remains `194`, schema revision/hash remain `6` / `d19707f043dc194b43c45578c2809b48339759bd6290f1393c666e05e18c9e5c`, focused V8 tests passed (`64` run, `63` passed, `1` optional pyPPG hook skipped), full unittest discovery passed (`260` run, `259` passed, `1` skipped), final smoke/audit-enabled caches are under `/tmp/waveform_feature_cache_smoke_v8_final_cleanup/v8/`, and runtime was `31.72 s` per 20-minute smoke sample. V8 is frozen; next step is full extraction/QC including larger severe-pressure audit. See `docs/v7_extracted_features/v8_supplemental_features.md`.
- Froze V8 at schema revision `6` after the final targeted correctness pass: duration-first continuous-run selection with deterministic latest-run tie-breaks, continuous-run-local baroreflex coverage, pairable-only baroreflex sequence denominator, no opposite-polarity ECG morphology fallback, JPG polynomial-order validation, simplified PLETH plateau logic, and observability-aware timing/pulse-deficit pairing. Schema hash `d19707f043dc194b43c45578c2809b48339759bd6290f1393c666e05e18c9e5c`; feature count remains `194`. Focused V8 tests passed (`63` run, `62` passed, `1` optional pyPPG hook skipped), full unittest discovery passed (`254` run, `253` passed, `1` skipped), and compileall passed. Frozen default smoke cache: `/tmp/waveform_feature_cache_smoke_v8_frozen/v8/smoke_local_v8_frozen`; audit-enabled smoke cache: `/tmp/waveform_feature_cache_smoke_v8_frozen/v8/smoke_local_v8_frozen_audit_enabled`; both shape `(2, 20, 194)` with v7-prefix alignment passing. See `docs/v7_extracted_features/v8_supplemental_features.md`.
- Updated the v7 extracted-feature regression Bland-Altman table to show two rows per target, best completed v7 model and `PatchTST v1 raw waveform`, and removed train-mean null rows. See `docs/v7_extracted_features/extractedFeaturesRegression.md`.
- Clarified in both regression docs that v7 and full-data regression use the same `gap` target semantics, while differing in cohort and alignment machinery. See `docs/v7_extracted_features/extractedFeaturesRegression.md` and `docs/full_data/extractedFeaturesRegressionFullData.md`.
- Clarified in both classification docs why legacy `anchor_horizon_filtered` labels differ from corrected full-data onset labels even after active-input filtering, including the late-in-horizon sustained-event example. See `docs/v7_extracted_features/extractedFeaturesClassification.md` and `docs/full_data/extractedFeaturesClassificationFullData.md`.
- Completed a targeted V8 correctness/robustness pass in place: APG `c/d` fallback now uses JPG zero crossings or returns missing, derivative finite-run edges are masked, robust jump thresholds use median plus MAD scale, V8-local arbitrary-unit scaling avoids the shared `1.0` floor for gain-invariant paths, PLETH-derived respiration normalizes after continuous-run selection, HRV/RESP/baroreflex coverage gates were tightened, RR irregularity uses one continuous RR population, ECG morphology refinement is polarity-aware, PLETH fiducial cache is config-keyed, pulse-deficit matching requires the same observed target run, quality spectral energy is duration-weighted, PLETH plateau/quantization are run-safe/gain-invariant, ABP area ratio uses a common linear baseline, baroreflex sequence counts are unique by SBP sequence, tau rebound fallback uses the refined peak, ABP successive-diff 5-minute features use one continuous run, and detector dictionary shape checks fail conservatively. Schema revision is `5`, hash `96eb6c928b3f3dbc3b638ef95e56a4be644c390640a0a0f85696544817c9428c`. Focused tests: `56` run, `55` passed, `1` optional pyPPG hook skipped; compileall passed. New 2-sample smoke cache: `/tmp/waveform_feature_cache_smoke_v8_final_v8_pass/v8/smoke_local_v8_final_v8_pass`, shape `(2, 20, 194)`, v7-prefix alignment passed. See `docs/v7_extracted_features/v8_supplemental_features.md`.
- Completed final V8 freeze-review fixes in place: conservative PLETH peak/notch/diastolic-peak validation, full-finite-run VPG/APG derivatives, explicit run-ID fail-closed logic, RESP continuous-run gating, corrected rolling-rhythm window semantics, ABP/PLETH 5-minute coverage gates, decoupled normalized dP/dt from notch success, removed dead `abp_flush_like_fraction`, and bumped schema revision to `4` with hash `d9cf3588f377d403016ba630f9fa321a809172f2621b5143a070677e90c8dd35`. Focused tests: `48` run, `47` passed, `1` optional pyPPG validation hook skipped. Final-freeze default/audit-enabled smoke caches are under `/tmp/waveform_feature_cache_smoke_v8_final_freeze/v8/`, both shape `(2, 20, 194)` with v7-prefix alignment passing and zero v7/v8 feature-name overlap. See `docs/v7_extracted_features/v8_supplemental_features.md`.
- Fixed the follow-up V8 correctness issues in place: pyPPG-ordered PLETH derivative fiducials with feature-specific validity and separate gating, contiguous-run morphology dynamics, conventional CV semantics, config-backed RESP pauses/RRV spectrum, tightened ABP notch/diastolic-peak/tau handling, schema revision `3` with hash `d5c4e1dffaf38fdace5745b5693cbc9f6e28c189553336586b6f581a4e4f2cbe`, and refreshed 2-sample default/audit-enabled smoke audits. Focused tests: `45` run, `44` passed, `1` optional pyPPG validation hook skipped. See `docs/v7_extracted_features/v8_supplemental_features.md`.
- Generated Bland-Altman plots and an agreement summary table for the documented best v7 extracted-feature regression model per target. Artifacts are in `blandaltman_v7_extractedfeatures/`; the summary table was inserted into `docs/v7_extracted_features/extractedFeaturesRegression.md`. See `docs/v7_extracted_features/bland_altman_analysis.md`.
- Expanded the existing supplemental v8 waveform extractor in place with literature-driven compact additions from Pal et al. ABP/PPG landmarks, pyPPG VPG/APG fiducials, and shock-prediction ABP/RESP dynamics while keeping v7 frozen and preserving v8 cache alignment. New schema has `195` v8 features, schema revision `2`, hash `cc65680f4ab33575d9314501a00d17fee86ed7d45a5a31e756852663c1982701`; focused tests passed (`38` tests), and 2-sample default/audit-enabled smoke caches are under `/tmp/waveform_feature_cache_smoke_v8_literature/v8/`. See `docs/v7_extracted_features/v8_supplemental_features.md`.
- Reworked the v7 extracted-feature classification filtered-target analysis to mirror the full-data table structure where possible while preserving the legacy `anchor_horizon_filtered` semantics. See `docs/v7_extracted_features/extractedFeaturesClassification.md`.
- Clarified why full-data extracted-feature classification metrics are not directly comparable to the earlier v7 extracted-feature classification table: different cohort/split, corrected onset-label semantics, lower prevalence, and broader task difficulty. See `docs/full_data/extractedFeaturesClassificationFullData.md`.
- Fixed and resubmitted the failed full-data raw-waveform `patchtst_v1` jobs: new jobs use job-local TorchInductor/Triton/CUDA caches, PatchTST now accepts hypoxia when named event-target columns are available, and the regression submitter supports `FEATURE_LIST` for partial resubmission. Resubmitted failed regression targets as SLURM `26957101`-`26957119` and classification targets as `26957120`-`26957125`. See `docs/full_data/full_data_raw_waveform_models.md`.
- Fixed remaining supplemental v8 correctness issues before full extraction; this 142-column smoke was superseded by the later 195-column v8 schema revision `2`. See `docs/v7_extracted_features/v8_supplemental_features.md`.
- Corrected the supplemental v8 implementation before full extraction: v7-consistent ECG preprocessing, causal full-5-minute HRV, continuous RR-run handling, renamed SPV/PLETH/burden/coupling features, one-to-one PAT pairing, conservative RESP pause detection, conservative dicrotic-notch/tau logic, updated tests, regenerated smoke cache, and generated audit overlays. See `docs/v7_extracted_features/v8_supplemental_features.md`.
- Implemented the supplemental v8 waveform-feature package, cache/alignment utilities, extraction and audit scripts, synthetic tests, and a 2-sample real-data smoke cache aligned to the frozen full-data v7 prefix. See `docs/v7_extracted_features/v8_supplemental_features.md`.
- Completed session handoff review using actual repo state: checked docs organization, key raw-waveform files, SLURM queue, running PatchTST logs, PatchTST output artifacts, full-data extracted-feature classification artifacts, stale doc references, and script syntax for doc-writer defaults. See `docs/README.md`, `docs/full_data/full_data_raw_waveform_models.md`, and `docs/full_data/extractedFeaturesClassificationFullData.md`.
- Reorganized `docs/` by project/dataset into `v1_vasopressor_free/`, `v7_extracted_features/`, `full_data/`, and `reference/`, with mini README files and updated links. See `docs/README.md`.
- Moved full-data raw-waveform `patchtst_v1` regression/classification setup, normalization, validation, and submitted-job details into `docs/full_data/full_data_raw_waveform_models.md`; extracted-feature full-data docs now link to that consolidated topic doc.
- Prepared and reviewed a 4-channel raw-waveform full-data `patchtst_v1` regression path using segment-aware `anchor_id` alignment, full-data WFDB-backed loading, full-data waveform-normalization support, evaluation identity export, focused tests, real-data indexing/target-filtering/sample/forward-pass smokes, and SLURM submitters. Fixed a full-data grouping bug before training submission. See `docs/full_data/full_data_raw_waveform_models.md`.
- Confirmed the full-data regression batch finished: `squeue -u dk5565` showed no regression jobs active, and artifact checks found complete `history_xgb`, `full_sequence_xgb`, and `transformer` outputs for all `26` targets. Added patient-cluster bootstrap 95% CIs for MAE/RMSE/R2 using `10000` replicates and seed `42`; refreshed result tables and saved JSON/CSV summaries. See `docs/full_data/extractedFeaturesRegressionFullData.md`.
- Generated Bland-Altman plots for the best full-data extracted-feature regression model per target: `26` PNGs plus per-plot and combined JSON summaries in `blandaltman_full_features/`. See `docs/v7_extracted_features/bland_altman_analysis.md`.
- Added a full-data Bland-Altman agreement summary table with one target-independent perfect-prediction row and target-specific train-mean null-model rows, using `2000` patient-cluster bootstrap replicates and seed `42`. Machine-readable outputs are in `blandaltman_full_features/bland_altman_agreement_summary.{csv,json,md}`. See `docs/full_data/extractedFeaturesRegressionFullData.md`.
- Documented the full-data Bland-Altman agreement-summary command, outputs, train-mean null-model convention, and validation checks in `docs/v7_extracted_features/bland_altman_analysis.md`.
- Checked full-data classification submissions `26944638`-`26944643` and `26945429`-`26945440`: all failed quickly with exit code `1:0`, produced no result artifacts, and stderr shows requested event target names were not found in the target bundles. See `docs/full_data/extractedFeaturesClassificationFullData.md`.
- Fixed feature-model classification target lookup so the shared `TargetExtractor` accepts corrected full-data onset columns such as `hypotension_onset_within_5m` while preserving legacy `hypotension_within_5m` bundles. Syntax check, focused test, and full-data extractor smoke checks passed. See `docs/full_data/extractedFeaturesClassificationFullData.md`.
- Resubmitted all failed full-data classification jobs after the target-name lookup fix: hypotension jobs `26949866`-`26949871`, and tachycardia/hypoxia jobs `26949872`-`26949883`. Initial stdout from started tabular jobs reports `missing=0` target alignment. See `docs/full_data/extractedFeaturesClassificationFullData.md`.
- Completed a handoff artifact audit for full-data extracted-feature classification: all 18 resubmitted filtered jobs now have `metrics.json` and `test_predictions.npz`, and all 12 XGBoost jobs also have `model.pkl`; refreshed the result tables including history XGBoost. See `docs/full_data/extractedFeaturesClassificationFullData.md`.

### In Progress
- Exact segment-planned V8 extraction - SLURM `26968656` (`128` shards, job name `full_wf_v8_segplan`): latest check showed all `128` tasks running, `128` stdout logs, `128` empty stderr logs, and no completed shard directories or `_SUCCESS` files yet.
- Full-data 4-channel raw-waveform `patchtst_v1` regression jobs currently running: `26954512`, `26954513`, and `26957101`-`26957106`; resubmitted regression jobs `26957107`-`26957119` remain pending on `QOSMaxMemoryPerUser`. No new eval prediction exports were inspected in this handoff. See `docs/full_data/full_data_raw_waveform_models.md`.
- Resubmitted full-data raw-waveform `patchtst_v1` classification jobs `26957120`-`26957125` remain pending on `QOSMaxMemoryPerUser`; no classification eval outputs were inspected in this handoff. See `docs/full_data/full_data_raw_waveform_models.md`.
- One unrelated interactive CPU shell is running: SLURM `26965061` (`bash`).
- Git status could not be checked because `git` is not installed on this node.

### Next Steps
- Monitor exact segment-planned V8 array `26968656`; when all `128` shards finish successfully, merge with `sbatch slurm/merge_full_data_vasofree_waveform_features_v8.sh`, then run full-cache `scripts/audit_waveform_features_v8.py` QC on `full_data_vasopressor_free_waveform_features_v8_segment_plan`. See `docs/v7_extracted_features/v8_supplemental_features.md`.
- Monitor running full-data raw-waveform regression jobs `26954512`, `26954513`, and `26957101`-`26957106`, plus pending jobs `26957107`-`26957125`; after jobs complete, run eval so `test_predictions.npz` exists for downstream significance tests. See `docs/full_data/full_data_raw_waveform_models.md`.
- Use `outputs/targets/feature_targets_gap_full_data_hardened_v2.npz` for future full-data regression submissions.
- For full-data extracted-feature classification, optional next analysis is paired/statistical comparison across completed history XGBoost, full-sequence XGBoost, and Transformer prediction exports. See `docs/full_data/extractedFeaturesClassificationFullData.md`.

## 2026-08-31

### Completed
- Cancelled pending duplicate full-data classification target-build job `26937036`; `squeue -j 26937036` showed it no longer queued. Corrected classification target outputs remain available locally. See `docs/full_data/extractedFeaturesClassificationFullData.md`.
- Generalized the full-data extracted-feature classification submitter with `CLASSIFICATION_EVENTS` while preserving the default hypotension-only behavior, then submitted tachycardia+hypoxia filtered jobs for `history_xgb`, `full_sequence_xgb`, and `transformer`: SLURM `26945429`-`26945440`. See `docs/full_data/extractedFeaturesClassificationFullData.md`.
- Completed the session handoff audit at `2026-08-31 17:46 EDT`: classification target artifacts were present, classification jobs were pending with no result directories yet, regression artifact counts had advanced to `26/26` for `history_xgb`, `10/26` for `full_sequence_xgb`, and `26/26` for `transformer`, and former interactive shells `26938915`/`26939496` were no longer queued. See `docs/full_data/extractedFeaturesClassificationFullData.md` and `docs/full_data/extractedFeaturesRegressionFullData.md`.
- Decided not to resubmit the currently running full-data regression jobs after the hardened target diff, because the active and hardened bundles have identical target values, masks, row identities, target names, and feature spec. The hardened bundle should be used for future submissions for provenance/validation metadata. See `docs/full_data/extractedFeaturesRegressionFullData.md`.
- Built the second-pass hardened full-data regression target bundle at `outputs/targets/feature_targets_gap_full_data_hardened_v2.npz` without overwriting the active production bundle. It has shape `(1969515, 78)`, `128227907` valid values, a `1969515/1969515` current-source identity match rate, and identical target values/masks/identities/names/spec to `outputs/targets/feature_targets_gap_full_data.npz`. See `docs/full_data/extractedFeaturesRegressionFullData.md`.
- Finalized and ran corrected full-data anchor-onset v2 hypotension labels using canonical absolute output times, explicit feature/numeric timestamp basis validation, phase-specific timelines for 150-second anchors, per-target diagnostics, debug/full/audit outputs, and minute-validity sensitivity bundles. See `docs/full_data/extractedFeaturesClassificationFullData.md`.
- Built the current stricter filtered full-data anchor-onset v2 hypotension bundle with `strict-clean-horizon`, per-recording last-positive late cutoff, and exclusion of recordings with no confirmed hypotension onset. Output: `outputs/targets/event_targets_full_data_anchor_onset_v2_5m_10m_recording_lastpos_positive_recordings_filtered.npz`; audit CSV: `outputs/targets/event_targets_full_data_anchor_onset_v2_5m_10m_recording_lastpos_positive_recordings_filtered.audit.csv`. See `docs/full_data/extractedFeaturesClassificationFullData.md`.
- Cleaned the full-data classification target builder by factoring negative filtering into explicit helpers, making the most recent recording-last-positive/no-positive-recording filtered configuration the script default, narrowing requested-event diagnostics, adding direct filter/default tests, and refreshing the current target documentation. Focused target tests and a 2-segment filtered cleanup smoke build passed. See `docs/full_data/extractedFeaturesClassificationFullData.md`.
- Added corrected full-data tachycardia (`HR > 110`) and hypoxia (`SpO2 < 90`) event labels to `scripts/build_full_data_event_targets.py`, preserving hypotension defaults and outputs. Built and validated `outputs/targets/event_targets_full_data_anchor_onset_v2_5m_10m_tachycardia_hypoxia_recording_complete_scan_filtered.npz` with `1843822` valid cells and `753264` anchors with at least one valid target. See `docs/full_data/extractedFeaturesClassificationFullData.md`.
- Hardened the full-data classification filtered target path so `recording` means the complete source recording rather than a segment chunk, computes recording-level last confirmed MAP-only onset from a canonical complete-recording timeline spanning all numerics-bearing chunks, asserts final-negative invariants, and generated `outputs/targets/event_targets_full_data_anchor_onset_v2_5m_10m_recording_complete_scan_filtered.npz`. See `docs/full_data/extractedFeaturesClassificationFullData.md`.
- Updated `slurm/submit_feature_models_full_data_v7.sh` to use the complete-scan filtered classification bundle by default and submit both `5m` and `10m` hypotension horizons. Submitted filtered full-data classification jobs for `history_xgb`, `full_sequence_xgb`, and `transformer`. See `docs/full_data/extractedFeaturesClassificationFullData.md`.
- Finished a second hardening pass for the full-data regression target pipeline: source correlation order is verified from actual source metadata, missing `X_stats` source-order metadata is recorded as an assumption rather than a verified fact, feature-cache tensor/mask/metadata/_SUCCESS checks were added, strict integral `anchor_id` parsing and identifier validation were added, current-source identity audit was added, timing metadata is relationship-based, duplicate diagnostics were deduplicated, per-segment time-spacing diagnostics and optional match-rate guard were added, and post-write bundle audits now reopen saved outputs. Focused and full test discovery passed (`169` tests); a real-data smoke matched `12/12` current identities and showed expected segment-end horizon loss. See `docs/full_data/extractedFeaturesRegressionFullData.md`.
- Cancelled pending hardened target rebuild SLURM `26940116` and ran the hardened full-data regression target builder locally to `outputs/targets/feature_targets_gap_full_data_hardened_20260831.npz`. The hardened bundle has shape `(1969515, 78)` with `128227907` valid values. Diff against the active production bundle found identical `feature_targets`, `feature_mask`, row identities, input intervals, target names, and feature spec; only `split_labels` and richer metadata were added. No regression job resubmission is needed for target correctness. See `docs/full_data/extractedFeaturesRegressionFullData.md`.
- Hardened the full-data regression target builder: object string arrays load without mmap, cache identity is validated against `anchors.csv`, source matching is segment-aware by `(patient_id, seg_name, quantized_window_time)`, duplicate source keys fail, target feature/correlation order and shapes are validated, per-horizon diagnostics are richer, target bundles preserve split labels, and downstream feature-model target alignment now prefers `anchor_id` with segment-aware and unique legacy fallbacks. Syntax checks, focused tests, full discovery (`164` tests), and a two-segment real-data smoke passed. See `docs/full_data/extractedFeaturesRegressionFullData.md`.
- Checked full-data classification target-creation progress. Numerics extraction array `26931914` and merge `26931915` completed: `128/128` shard `_SUCCESS` markers exist, merged numerics `_SUCCESS` exists, and merged metadata reports `1969515` rows. See `docs/full_data/extractedFeaturesClassificationFullData.md`.
- Built the full-data segment-aware regression target bundle from readable `X_stats.npy`: `outputs/targets/feature_targets_gap_full_data.npz` has shape `(1969515, 78)` across the same `26` base targets and `0/20/60` minute gap horizons. MAP `t+0m_gap` alignment smoke check matched all feature-cache anchors by `anchor_id`. See `docs/full_data/extractedFeaturesRegressionFullData.md`.
- Submitted full-data `t+0m_gap` regression jobs for all `26` targets using `history_xgb`, `full_sequence_xgb`, and `transformer`. The initial XGBoost submissions `26934307`-`26934358` OOMed after successful target alignment; tabular training was patched to transform only valid split rows, use histogram XGBoost with `n_jobs=2`, and request `192G`, then XGBoost jobs were resubmitted as `26934627`-`26934678`. Transformer jobs `26934359`-`26934384` remain pending on GPU priority. See `docs/full_data/extractedFeaturesRegressionFullData.md`.
- Checked full-data regression job progress at `2026-08-31 14:58 EDT`: `history_xgb` has `5/26` completed and `21/26` running; `full_sequence_xgb` has `3/26` completed and `23/26` pending on priority; `transformer` has `2/26` completed, `1/26` running, and `23/26` pending on GPU priority. See `docs/full_data/extractedFeaturesRegressionFullData.md`.
- Checked full-data regression job progress at `2026-08-31 16:35 EDT`: `history_xgb` has `24/26` completed and `2/26` running; `full_sequence_xgb` has `3/26` completed, `12/26` running, and `11/26` pending; `transformer` has `16/26` completed, `3/26` running, and `7/26` pending. `sacct` was temporarily unavailable, so status was based on `squeue` plus output artifact presence. See `docs/full_data/extractedFeaturesRegressionFullData.md`.
- Updated full-data regression results documentation with separate partial tables for `history_xgb`, `full_sequence_xgb`, and `transformer`, plus a provisional best-completed-model table covering all `26` targets. See `docs/full_data/extractedFeaturesRegressionFullData.md`.

### In Progress
- Superseded by the `2026-09-01` status: full-data regression completed, and the full-data classification submissions failed before artifact creation.
- Git status could not be checked because `git` is not installed on this node. Files changed in this session include `scripts/build_full_data_event_targets.py`, `waveform_baselines/task_specs.py`, `tests/test_corrected_event_labeling.py`, `slurm/submit_feature_models_full_data_v7.sh`, `docs/full_data/extractedFeaturesClassificationFullData.md`, `docs/full_data/extractedFeaturesRegressionFullData.md`, and `PROGRESS.md`.

### Next Steps
- Use `outputs/targets/feature_targets_gap_full_data_hardened_v2.npz` for future full-data regression submissions; do not resubmit the completed regression batch solely for target correctness.
- Inspect full-data classification target-name metadata and submitter event-name mapping before resubmitting failed classification jobs.

## 2026-08-30

### Completed
- Prepared full-data extracted-feature regression/classification target builders, anchor-id-aware feature-model alignment, full-data model submitter, and requested result docs. Regression target creation is blocked by file-level `X_stats.npy` permissions. Classification now has aligned full-data numerics extraction/merge scripts, and a `5`-patient compatibility check showed exact old-vs-new numerics and unfiltered target equality on `3294` prior rows. See `docs/full_data/extractedFeaturesRegressionFullData.md`, `docs/full_data/extractedFeaturesClassificationFullData.md`, and `docs/full_data/full_data_new_windows.md`.
- Submitted full-data classification preparation chain: numerics extraction array `26931914` (`0-127`), merge job `26931915` with `afterok:26931914_*`, and event-target build job `26931916` with `afterok:26931915`.
- Reworded full-data cohort references across docs/code, renamed repo-local full-data extractor/merge scripts and SLURM wrappers, and added `full_data_*` symlinks to existing completed manifest/cache artifacts so renamed defaults resolve. See `docs/full_data/full_data_new_windows.md`.
- full-data vaso-free v7 extracted-feature array `26920063` and merge job `26920070` completed; all `128` shard `_SUCCESS` markers plus the merged cache `_SUCCESS` are present. The merged cache at `/gpfs/data/eh3828lab/derived_datasets/baselines/waveformBaselines/featureExtraction/full/v7/full_data_vasopressor_free_waveform_features_v7` has shape `(1969515, 20, 93)`. See `docs/full_data/full_data_new_windows.md`.

### In Progress
- Full-data classification preparation chain is active: numerics extraction array `26931914` (`0-127`) running; merge `26931915` pending on `afterok:26931914_*`; event-target build `26931916` pending on `afterok:26931915`. Latest check: `0/128` extraction shard `_SUCCESS` markers, `0` nonempty extraction stderr logs, no merged numerics, and no full-data classification target bundle yet.
- One unrelated interactive CPU shell is running: SLURM `26931652` (`bash`).
- Git working-tree status could not be checked because `git` is not installed on this node; recent repo files include the full-data target/numerics scripts, full-data docs, `PROGRESS.md`, and generated `__pycache__` files from verification.

### Next Steps
- Fix file-level read access or provide a readable replacement for full-data `X_stats.npy` (`ms17929:ms17929`, mode `770` at latest check), then run `sbatch slurm/build_full_data_feature_regression_targets.sh`.
- Monitor full-data classification jobs `26931914`, `26931915`, and `26931916`; after `26931914` completes, verify all `128` `_SUCCESS` markers and inspect nonempty stderr logs before merge/target outputs are trusted. After `26931916` completes, validate filtered target metadata/counts before launching classification models.
- After target bundles exist and are validated, submit full-data extracted-feature models with `slurm/submit_feature_models_full_data_v7.sh` and populate the full-data result docs.

## 2026-08-29

### Completed
- Built and hardened the full-data segment-level vasopressor-free waveform manifest for `/gpfs/data/eh3828lab/derived_datasets/physionet_restricted/mimic_derived_data/data_m3_120s_prediction`; canonical outputs are under `/gpfs/data/eh3828lab/derived_datasets/baselines/waveformBaselines/manifests/full_data_segment_level_vasopressor_free_*`. QC: `32044` segments, `21833` high-confidence vaso-free, `8658` confirmed overlap, `1553` unknown, `0` metadata/WFDB timestamp mismatches. See `docs/full_data/record_level_vasopressor_free_manifest.md` and `docs/full_data/full_data_new_windows.md`.
- Added and smoke-tested full-data-specific v7 extracted-feature scripts: `scripts/extract_full_data_vasofree_waveform_features.py`, `scripts/merge_full_data_waveform_feature_shards.py`, `slurm/extract_full_data_vasofree_waveform_features_array.sh`, and `slurm/merge_full_data_vasofree_waveform_features.sh`. Smoke extraction shape was `(2, 20, 93)` and smoke merge shape was `(4, 20, 93)`. The full expected full-data vaso-free extraction set is `1969515` windows. See `docs/full_data/full_data_new_windows.md`.
- Re-ran the focused vasopressor-manifest tests after the final handoff review: `/gpfs/data/eh3828lab/derived_datasets/baselines/conda/myenv/bin/python -m unittest tests.test_record_level_vasopressor_free_manifest` passed `30` tests.
- Completed TCN-only PatchTST-relative significance analyses: regression SLURM `26919471` and classification SLURM `26919472`; inserted TCN rows into extracted-feature result docs.
- Completed TCN extracted-feature training/export jobs `26919397`-`26919423`; artifacts include 26 regression and 1 classification `metrics.json`/`test_predictions.npz` outputs under `/gpfs/data/eh3828lab/derived_datasets/baselines/waveformBaselines/featureExtractedModels`. Backfilled deterministic `sample_ids` into 17 early TCN regression exports for paired significance testing.
- Audited extracted-feature sequence-model implementations and training/data path; added config validation, seeded Torch shuffling, parameter/receptive-field metadata, richer prediction artifacts, and focused tests. See `docs/v7_extracted_features/extractedFeaturesModelDescription.md`.
- Implemented `tcn` in the extracted-feature training framework with causal dilated residual Conv1d blocks, prediction export support, SLURM submitters, significance-script support, documentation, and focused causality tests.
- Removed adjusted p-value columns and Holm-adjustment wording from the classification results documentation.
- Clarified in the extracted-feature result docs that CI and p-value columns are PatchTST-relative row-wise comparisons, including the frequentist interpretation of 95% CIs.
- Updated `docs/v7_extracted_features/extractedFeaturesClassification.md` with completed MLP-inclusive classification significance results from SLURM `26919239`.
- Added PatchTST-relative CI and p-value columns to the `Best Completed v7 Per Target` table in `docs/v7_extracted_features/extractedFeaturesRegression.md`.
- Reformatted `docs/v7_extracted_features/extractedFeaturesRegression.md` statistical comparisons into the requested per-model target tables with CI/p-value columns adjacent to each PatchTST-vs-v7 delta.
- Populated statistical comparison tables in `docs/v7_extracted_features/extractedFeaturesRegression.md` and `docs/v7_extracted_features/extractedFeaturesClassification.md`; both regression and classification now include full-sequence MLP significance results.
- Added completed `full_sequence_mlp` results to `docs/v7_extracted_features/extractedFeaturesRegression.md` and `docs/v7_extracted_features/extractedFeaturesClassification.md`; MLP does not become the top regression model for any target and underperforms PatchTST on filtered 5m classification. Current best-model counts are documented in `docs/v7_extracted_features/extractedFeaturesRegression.md`.
- Added completed `full_sequence_xgb` results to `docs/v7_extracted_features/extractedFeaturesRegression.md` and `docs/v7_extracted_features/extractedFeaturesClassification.md`; after exact comparison in the expanded best-model table, full-sequence XGBoost is best for 6/26 regression targets and has the highest filtered 5m classification specificity at 85% sensitivity.
- Added a prepared `full_sequence_mlp` extracted-feature model path that flattens `(20, 186)` to `(N, 3720)`, trains a feed-forward neural network, exports test predictions, and has regression/classification SLURM submitter support. See `docs/v7_extracted_features/extractedFeaturesModelDescription.md`.
- Added a prepared `full_sequence_xgb` extracted-feature model path that flattens the full `(20, 186)` preprocessed sequence to `(N, 3720)`, exports test predictions, and has regression/classification SLURM submitter support. See `docs/v7_extracted_features/extractedFeaturesModelDescription.md`.
- Clarified `docs/v7_extracted_features/extractedFeaturesModelDescription.md` input-shape conventions, including the meaning of `N` and the exact per-model input tensors/matrices.
- Added aligned `test_predictions.npz` export to `scripts/train_feature_models.py`, plus `model.pkl` persistence for tabular feature models, to support paired significance testing.
- Audited artifacts for the requested PatchTST-vs-v7 paired regression significance analysis; CIs/p-values are blocked because v7 per-test predictions were not saved. See `docs/v7_extracted_features/extractedFeaturesRegression.md`.
- Added `docs/v7_extracted_features/extractedFeaturesModelDescription.md` describing the v7 extracted-feature persistence, XGBoost, GRU, and Transformer models used for regression and classification.
- Implemented exact all-target persistence support for v7 regression by aligning current-anchor values from the target-generation source table; smoke checks passed for `HR` and `PLETH_ACDC_PLETH_amp`. Completed the 25 missing persistence jobs `26918444`-`26918468`; existing MAP persistence was skipped. See `docs/v7_extracted_features/extractedFeaturesRegression.md`.
- v7 extraction array `26873594` and merge `26873626` completed; all 32 shards plus the merged v7 cache have `_SUCCESS` markers. See `docs/v7_extracted_features/v7_feature_job_status_2026-08-29.md` and `docs/v7_extracted_features/extractedFeatures.md`.
- Merged v7 cache: `/gpfs/data/eh3828lab/derived_datasets/baselines/waveformBaselines/featureExtraction/v7/vasopressor_free_waveform_features_v7`, shape `(334833, 20, 93)`.
- Fixed v7 feature-model target alignment by rounding `(patient_id, anchor_time)` joins to 6 decimal places; production cache now aligns to regression and filtered classification target bundles with zero missing rows. A local persistence smoke completed. See `docs/v7_extracted_features/extractedFeatures.md`.
- Completed the v7 full-cohort feature-quality audit summary. No zero-valid features were found, but the ECG/ABP rate-disagreement tail remains a quality caveat. See `outputs/feature_models/v7_full_cohort_feature_quality_audit_2026-08-29.json` and `docs/v7_extracted_features/extractedFeatures.md`.
- Completed resubmitted v7 MAP regression jobs `26898023`, `26898027`, `26898042`, `26898048` and filtered 5m hypotension classification jobs `26898025`, `26898024`, `26898044`, `26898046`; all reported zero missing target rows. History XGBoost is the best filtered 5m hypotension classifier. Metrics exist but should be interpreted with the v7 ECG/ABP audit caveat.
- Completed v7 all-target `t+0m_gap` extracted-feature regression jobs for the same 26 targets as `docs/v1_vasopressor_free/regression_results_v1_vaso_free_sorted.md`; current-state XGBoost, history XGBoost, GRU, and Transformer are complete for `26/26`. History XGBoost is now best on `20/26` targets, and duplicate MAP jobs `26898026`/`26898383` produced identical metrics/log summaries. See `docs/v7_extracted_features/extractedFeaturesRegression.md`.
- Added v7 extracted-feature result tables comparing completed regression/classification runs against prior vasopressor-free `patchtst_v1` results. See `docs/v7_extracted_features/extractedFeaturesRegression.md` and `docs/v7_extracted_features/extractedFeaturesClassification.md`.

### In Progress
- Git working-tree status could not be checked because `git` is not installed on this node.

### Next Steps
1. Build segment-aware full-data target/split artifacts keyed by `anchor_id` and `segment_id`; do not reuse old `outputs/splits/vasopressor_free_splits.json` or old target bundles for full-data jobs.
2. Resolve or confirm readable schema for full-data `X_stats.npy` before regression target construction.
3. Locate or generate numerics windows aligned to the full-data anchor grid before classification target construction.
4. Adapt extracted-feature regression/classification submitters to the full-data cache and new target paths only after the merged feature cache and target bundles are verified.
5. Continue existing v7 model-analysis follow-up: review TCN narrative, add classification calibration/threshold diagnostics, and add selected Bland-Altman/predicted-vs-observed diagnostics.

## 2026-08-28

### Completed
- Hardened v7 sharded waveform-feature extraction and merge integrity without changing physiological algorithms: canonical pre-shard anchor ordering, global uniqueness before sharding, zero-padded unique shard outputs, `_SUCCESS` markers, strict shard/config/coverage merge validation, recomputed merged quality reports, summed ECG diagnostics, and downstream completion-marker checks. Focused waveform tests passed (`61` tests), full discovery passed (`87` tests), and compileall passed. See `docs/v7_extracted_features/extractedFeatures.md`.
- Reproduced v5 ECG over-detection on a 60-bpm synthetic ECG (`180` detections, `178.57` bpm), implemented v6 final corrections, then applied the final v7 ECG correction so XQRS receives gap-safe raw calibrated ECG, detector selection/fallback are explicit, and detector diagnostics are saved. v7 focused waveform tests passed (`56` tests), full discovery passed (`82` tests), compileall passed, and a 4-sample v7 smoke cache plus detector plots were generated. See `docs/v7_extracted_features/extractedFeatures.md`.
- Performed the final focused v4 waveform-feature audit, confirmed remaining numerical issues, implemented v5 corrections, passed 39 focused waveform tests and 65 full-discovery tests, generated a 4-sample v5 smoke cache, inspected overlay plots, cancelled superseded v4 jobs, and submitted v5 extraction/dependent jobs. See `docs/v7_extracted_features/extractedFeatures.md`.
- Performed the final focused v3 waveform-feature audit, confirmed numerical issues, implemented v4 corrections, passed 33 focused waveform tests and 59 full-discovery tests, generated a 4-sample real-data smoke cache, and inspected overlay plots. See `docs/v7_extracted_features/extractedFeatures.md`.
- Audited the v2 waveform-feature pipeline and implemented v3 corrections for trough segmentation, bounded missing-data interpolation, HRV adjacency, RESP validity fractions, missing-vs-flatline SQI, and history slopes. v3 passes 23 focused tests and a two-sample real-data smoke extraction with overlay plots. See `docs/v7_extracted_features/extractedFeatures.md`.
- Audited `/gpfs/data/eh3828lab/derived_datasets/physionet_restricted/mimic_derived_data/data_m3_120s_prediction`, matched its segment keys to `mimic3_waveforms_matched`, confirmed the new dataset has `2,056` patients and `2,847,597` windows, and documented the required regression/classification dataset-construction steps plus current blockers in `docs/full_data/full_data_new_windows.md`.
- Added optional train-only regression target z-scoring to `scripts/train_patchtst.py` / `scripts/eval_patchtst.py`, with saved `target_normalization.json`, checkpoint fallback metadata, original-unit metric reporting after inverse transform, and a submitter flag for the existing vasopressor-free `patchtst_v1` batch. See `docs/v1_vasopressor_free/training.md` and `docs/v1_vasopressor_free/training_vaso_free.md`.
- Added focused target-normalization tests covering train-only stats, val/test reuse of training stats, inverse transform, and inference-time sidecar reuse.
- Syntax-checked the edited Python and SLURM files, then ran `/gpfs/data/eh3828lab/derived_datasets/baselines/conda/myenv/bin/python -m unittest tests.test_target_normalization tests.test_eval_patchtst_config_loading`; the focused target-normalization/config suite passed (`10` tests).
- Characterized the train-split distributions of the `26` leakage-safe vasopressor-free `t+0` regression targets, added split-shift comparisons against val/test, rendered histograms/central histograms/boxplots/ECDFs, and documented the results in `docs/v1_vasopressor_free/target_distributions.md`. Reproducible artifact: `outputs/targets/target_distributions_vasopressor_free_t0.json`; figures: `docs/figures/target_distributions/`.
- Extended `docs/v1_vasopressor_free/target_distributions.md` with train-only transformation analysis for the same `26` targets, including `z`-score, selective `log1p`, selective Box-Cox, and Yeo-Johnson comparisons, fitted lambdas, raw-vs-transformed plots, and per-target recommendations for future regression experiments. Reused `outputs/targets/target_distributions_vasopressor_free_t0.json` and added transform-comparison figures under `docs/figures/target_distributions/transformations/`.
- Completed GPU evaluation for `outputs/patchtst/vasopressor_free_v1_target_norm_es/` as SLURM `26869390` (`ptst_eval_vf_norm`); all `26` normalized-target runs now have `test_metrics.json` artifacts and a shared summary at `outputs/patchtst/vasopressor_free_v1_target_norm_es/test_results_summary.json`. See `docs/v1_vasopressor_free/regression_results_v1_vaso_free_target_normalized.md` and `docs/v1_vasopressor_free/training_vaso_free.md`.
- Audited and corrected the waveform-feature extraction pipeline against nine methodology issues, including duplicate SDNN removal, ECG morphology-signal separation, RESP high-rate recovery, SQI validity fixes, and leave-one-out morphology consistency. The corrected representation is `v2` with shape `(20, 90)`. See `docs/v7_extracted_features/extractedFeatures.md`, `docs/v7_extracted_features/extractedFeaturesClassification.md`, and `docs/v7_extracted_features/extractedFeaturesRegression.md`.
- Expanded waveform-feature tests to cover ABP raw-pressure preservation, RESP recovery through `60` breaths/min, morphology-consistency behavior, leakage safety, preprocessing determinism, and model smoke shapes. Local command passed: `/gpfs/data/eh3828lab/derived_datasets/baselines/conda/myenv/bin/python -m unittest tests.test_waveform_feature_pipeline` (`16` tests).
- Regenerated a corrected `2`-sample temporary real-data smoke cache at `/tmp/waveform_feature_cache_smoke_v2/v2/smoke_local_v2` and rechecked representative ABP/ECG/PLETH values plus beat-mean MAP behavior against raw ABP minute means.

### In Progress
- Superseded v5 waveform-feature jobs `26872137`-`26872146` were cancelled after confirmed ECG over-detection and detector/config fixes. Serial v6 extraction/dependent jobs `26873175`, `26873177`-`26873185` were cancelled before completion after the final v7 ECG XQRS-input/provenance correction. Pre-hardening v7 jobs `26873436`, `26873437`, `26873469`-`26873477` were cancelled; corrected v7 extraction array `26873594`, merge `26873626`, and dependent jobs `26873627`-`26873635` were still running or pending at the `2026-08-28` handoff. See `docs/v7_extracted_features/extractedFeatures.md`.
- Latest `squeue` check on `2026-08-28` showed one non-repo-specific interactive CPU shell still running: SLURM `26872266` (`cpu_dev`, job name `bash`). Git status was not verifiable because `git` is unavailable on this node.

### Next Steps
1. Compare the new original-unit regression metrics against the existing non-target-normalized `vasopressor_free_v1_es` results and decide which normalized targets merit reruns.
2. Use `docs/v1_vasopressor_free/target_distributions.md` to decide, target by target, whether future regression reruns should test only z-scoring or also nonlinear transforms.
3. When regression retraining resumes, test the recommended candidate transforms first: `log1p` for `DBP`, `MAP`, and `PVI`; Box-Cox for `ABP_area`, `ShockIdx`, `PPV`, and `dPdt_max`; Yeo-Johnson for `PLETH_ACDC`, `ECG_Ramp`, `PTT`, `ABP_tau`, `RESP_amp`, and the flagged bounded-correlation targets.
4. For the full-data `2056`-patient `data_m3_120s_prediction` cohort, resolve `X_stats.npy` read access, locate or build aligned numerics arrays, and implement a segment-aware waveform/anchor pipeline before attempting new v1 regression or classification runs. See `docs/full_data/full_data_new_windows.md`.
5. Continue v7 follow-up from the `2026-08-29` status: resolve feature-cache/target-bundle anchor alignment, resubmit dependent jobs, and complete the train/validation feature-quality audit before interpreting downstream model results. Treat v7 as frozen after that audit unless it shows a concrete empirical failure.

## 2026-08-27

### Completed
- Implemented and hardened `patchtst_v1_5`, a supervised PhysioJEPA-style PatchTST variant with grouped-Conv tokenization, rotary self-attention, attentive pooling, a strict `--physiojepa-fidelity` preset, eager-only execution, tokenizer-derived patch counts, and dedicated v1.5 tests. See `docs/v1_vasopressor_free/training.md`, `docs/v1_vasopressor_free/model_description.md`, and `docs/reference/data_description.md`.
- Added `docs/v1_vasopressor_free/model_description.md` to compare `patchtst_v1`, `patchtst_v1_5`, and `patchtst_v2`, including their key architecture differences and typical configs.
- Added `docs/reference/data_description.md` to summarize waveform inputs, split-specific normalization, regression targets, and event-label bundles.
- Clarified in the docs that current `patchtst_v1` and `patchtst_v1_5` results use `3` waveform channels (`ABP`, `II`, `PLETH`), while `patchtst_v2` is the `4`-channel experimental path. See `docs/reference/data_description.md`, `docs/v1_vasopressor_free/training.md`, `docs/v1_vasopressor_free/classification_results_v1_vaso_free.md`, and `docs/v1_vasopressor_free/regression_results_v1_vaso_free_sorted.md`.
- Tightened the wording so `patchtst_v2` is documented as the repo's `4`-channel experimental variant by default, unless a run explicitly overrides `--channels`. See `docs/reference/data_description.md` and `docs/v1_vasopressor_free/training.md`.
- Reorganized the docs to remove long histories, duplicate interpretation, and stale queue notes. See `docs/README.md`.
- Added early stopping to `scripts/train_patchtst.py` and documented the trainer and submitter behavior. See `docs/v1_vasopressor_free/training.md`.
- Replaced the mistaken standard-cohort reruns with fresh early-stopping-enabled vasopressor-free `patchtst_v1` jobs. See `docs/v1_vasopressor_free/training_vaso_free.md`.
- Deleted the unused `anchor_event_centric_physiojepa_style` event-target mode from the code path and CLI, and added `anchor_horizon_filtered` for stricter hypotension negatives. See `docs/v1_vasopressor_free/target_generation.md`.
- Completed regression and classification evaluation for the fresh vasopressor-free `patchtst_v1` runs, including the filtered-label hypotension GPU eval jobs `26851239` and `26851240`. See `docs/v1_vasopressor_free/regression_results_v1_vaso_free_sorted.md`, `docs/v1_vasopressor_free/classification_results_v1_vaso_free.md`, and `docs/v1_vasopressor_free/training_vaso_free.md`.
- Added exact positive/valid counts behind the vasopressor-free classification prevalences for filtered and non-filtered `5m` and `10m` tables. See `docs/v1_vasopressor_free/classification_results_v1_vaso_free.md`.
- Added per-condition negative-removal counts for filtered vasopressor-free `5m` and `10m` hypotension labels, including outcome-window validity/event overlap. See `docs/reference/data_description.md`.
- Added the corresponding test-split filtering breakdown and reconciled it with the filtered classification denominators. See `docs/reference/data_description.md` and `docs/v1_vasopressor_free/classification_results_v1_vaso_free.md`.
- Deleted 44 center-mode `t+0` feature-regression run directories that used overlapping input/target windows; retained the shared `all_targets.npz` bundle and all gap-mode runs.
- Deleted obsolete event-centric target artifacts, the unreferenced WFDB training loader, and four old/leakage-prone submission/build scripts. Retained `slurm/train_patchtst.sh` because current gap-mode and event submitters use it as a shared runner.
- Fixed `scripts/eval_patchtst.py` so checkpoint-saved training config is now the default source of truth for dataset paths, splits, target bundle, task selection, and model architecture, with explicit CLI overrides only when the user actually supplies them. Also replaced permissive eval-time `strict=False` loading with `_orig_mod.` prefix stripping plus strict checkpoint loading, and added regression tests for the config-merge and model-mismatch cases. See `docs/v1_vasopressor_free/evaluation.md`.

### In Progress
- No training or evaluation SLURM jobs are currently pending.
- Last successful `squeue` check observed one non-repo-specific interactive CPU shell running: SLURM `26851827`; a later status query could not connect to Slurm from this environment, so verify before treating it as still active.
- The optional standard event target artifact `outputs/targets/event_targets_standard_5m_10m.npz` is absent and is not used by current models; rebuild it only if standard-cohort event experiments resume.
- Python `compileall` passed; runtime unit-test execution is still blocked in the active environment because the available Python lacks `torch`, and the `git` executable was unavailable, so Git working-tree status was not independently verifiable.
- Root-level `patch_probe_tmp.txt` and empty `stats.ipynb` were left untouched because their ownership/purpose was not established.

### Next Steps
1. Compare the fresh vasopressor-free `patchtst_v1` regression and classification results against the older `pre-v1` baselines; treat `docs/v1_vasopressor_free/evaluation.md` regression results as legacy because their center-mode `t+0` targets overlap the input.
2. Decide whether to launch strict `patchtst_v1_5 --physiojepa-fidelity` supervised runs on the vasopressor-free cohort; no such runs have been launched.
3. If `v1_5` is promoted, create a submitter that explicitly pins the current leakage-safe target bundles and document the jobs in `docs/v1_vasopressor_free/training_vaso_free.md`.
4. Rebuild and verify the missing standard `5m`/`10m` event bundle only if standard-cohort event experiments are needed.
5. Re-run the new evaluation-config regression tests in a Python environment with `torch` installed.

## 2026-08-26

### Completed
- Rebuilt and verified vasopressor-free event targets with minute-level aggregation and current `5`-minute event semantics. See `docs/v1_vasopressor_free/target_generation.md`.
- Added bootstrap event-metric confidence intervals and refreshed evaluation summaries. See `docs/v1_vasopressor_free/evaluation.md`.
- Added target-stat summaries and refreshed Bland-Altman notes. See `docs/v1_vasopressor_free/target_statistics.md` and `docs/v7_extracted_features/bland_altman_analysis.md`.
- Added `patchtst_v2`, shared normalization, and standard `5m`/`10m` event-target support. See `docs/v1_vasopressor_free/training.md` and `docs/v1_vasopressor_free/target_generation.md`.

### Next Steps
- Compare vasopressor-free results against PhysioJEPA.
- Decide whether to expand `patchtst_v2` beyond pilot status.

## 2026-08-25

### Completed
- Finished the clean vasopressor-free PatchTST batch and evaluated the saved models. See `docs/v1_vasopressor_free/training_vaso_free.md`.
- Added vasopressor-free regression, event, and threshold-sweep summaries. See `docs/v1_vasopressor_free/training_vaso_free.md`.

### Next Steps
- Compare against PhysioJEPA.
- Decide whether to submit the vasopressor-free `t+20m` gap-mode regression batch.

## 2026-08-24

### Completed
- Prepared the vasopressor-free overlap cohort assets and fixed the regression `NaN` issues. See `docs/v1_vasopressor_free/training_vaso_free.md`.
- Resubmitted the clean vasopressor-free regression and event batches. See `docs/v1_vasopressor_free/training_vaso_free.md`.

## 2026-08-23

### Completed
- Evaluated the original full-cohort PatchTST baseline and computed simple baselines. See `docs/v1_vasopressor_free/evaluation.md`.

### Next Steps
- Compare PatchTST against PhysioJEPA.
- Extend to later horizons for the strongest targets.

## 2026-08-19 to 2026-08-21

### Completed
- Built the initial documentation, target bundles, splits, dataset pipeline, and single-target PatchTST trainer. See `docs/v1_vasopressor_free/training.md` and `docs/v1_vasopressor_free/target_generation.md`.
- Replaced the original WFDB-at-train-time path with the NumPy/mmap pipeline and stabilized training after several failed early batches. See `docs/v1_vasopressor_free/training.md`.
